# NAAS Toolkit local CLI environment template
# Copy to .env for local runs:
#   Copy-Item .env.example .env

# Optional profile fallback when --profile is not provided
NAAS_PROFILE=preprod

# Optional: override state output directory (default: ./state)
NAAS_STATE_DIR=state

# Optional: fallback desired spec when configs/<profile>.yaml is missing
# Example for --profile preprod -> specs/preprod.desired.yaml
NAAS_DESIRED_SPEC=specs/preprod.desired.yaml

# Proxmox endpoint (required for env-only mode)
PROXMOX_ENDPOINT=https://proxmox.local:8006

# Proxmox auth: use either token pair OR username/password
# Token auth (recommended)
PROXMOX_TOKEN_ID=naas@pve!naasctl
PROXMOX_TOKEN_SECRET=__REPLACE_WITH_TOKEN_SECRET__

# OR user/password auth
# PROXMOX_USERNAME=root@pam
# PROXMOX_PASSWORD=__REPLACE_WITH_PASSWORD__

# Optional Docker/Grafana
DOCKER_HOST=tcp://127.0.0.1:2375
GRAFANA_ENDPOINT=https://grafana.local
GRAFANA_TOKEN=__REPLACE_WITH_GRAFANA_TOKEN__

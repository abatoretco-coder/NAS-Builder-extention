{
  "name": "naas-assistant",
  "private": true,
  "version": "0.1.0",
  "description": "MVP NAS assistant monorepo (CLI + VS Code extension)",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "package": "pnpm --filter naas-toolkit package"
  },
  "devDependencies": {
    "@eslint/js": "^9.21.0",
    "@types/node": "^20.17.24",
    "eslint": "^9.21.0",
    "typescript-eslint": "^8.25.0",
    "typescript": "^5.7.3"
  }
}